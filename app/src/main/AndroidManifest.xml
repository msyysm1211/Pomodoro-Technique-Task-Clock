<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.msy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ClockActivity"></activity>

        <service
            android:name=".MyIntentService"
            android:exported="false" />

        <activity android:name=".Tabthree2Activity" />
        <activity android:name=".TabtwoActivity" />
        <activity android:name=".AddTaskActivity" />
        <activity android:name=".AllClockActivity" />
        <activity android:name=".test" />
        <activity android:name=".NavigationTest" />
        <activity
            android:name=".ui.main.Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".MyIntentService"
            android:exported="false"></service>
    </application>

</manifest>